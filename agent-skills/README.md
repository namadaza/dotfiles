# Agent Skills

Used to enhance LLM tools i.e. Claude Code.

Stores code from repos under `~/.agents`

Update installed agent skills:

```bash
./scripts/agent-skills-update-config.sh
```

```bash
npx skills add vieko/bonfire
```
